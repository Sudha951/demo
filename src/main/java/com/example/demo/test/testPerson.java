package com.example.demo.test;

import java.util.UUID;

import com.example.demo.model.person;

public interface testPerson {
    public int addPersson(UUID id, person p);
 
     int addPersson(person p);

}
